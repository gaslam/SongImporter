name: Windows GUI Release
on:
  push:
    branches:
      - 'main'
      - 'dev'
    tags:
      - 'v*'
  pull_request:
    branches:
    - 'main'
    - 'dev'

    
defaults:
  run:
    shell: cmd

env:
  SOURCE_DIR:   ${{ github.workspace }}
  QT_VERSION:   6.10.1
  ARTIFACT:     songimporter.zip
jobs:

